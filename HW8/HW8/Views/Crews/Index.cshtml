@model IEnumerable<HW8.Models.Crews>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*List the Crews*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pirates.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ships.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Booty)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pirates.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ships.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Booty)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
